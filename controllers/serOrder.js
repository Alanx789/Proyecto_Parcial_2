const FB = require('../firebase/index')
const db = {FB}

const setOrder = async(req, res) => {
    try {
        
    } catch (error) {
        res.send(error)
    }
}

module.exports = {
    setOrder
}